import * as React from 'react';

import { Text, View } from '@/ui';

type Props = {};
export const Wishlist = ({}: Props) => {
  return (
    <View className="flex-1">
      <Text className="text-base">Wishlist Component</Text>
    </View>
  );
};
