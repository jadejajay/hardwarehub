import * as React from 'react';

import { Text, View } from '@/ui';

type Props = {
  
};
export const PostScreen = ({}: Props) => {
  return (
    <View className="flex-1">
      <Text className="text-base">PostScreen Component</Text>
    </View>
  );
};
