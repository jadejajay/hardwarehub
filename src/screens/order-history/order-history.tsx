import * as React from 'react';

import { Text, View } from '@/ui';

type Props = {};
export const OrderHistory = ({}: Props) => {
  return (
    <View className="flex-1">
      <Text className="text-base">OrderHistory Component</Text>
    </View>
  );
};
