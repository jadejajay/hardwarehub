import * as React from 'react';

import { Text, View } from '@/ui';

type Props = {};
export const EditPost = ({}: Props) => {
  return (
    <View className="flex-1">
      <Text className="text-base">EditPost Component</Text>
    </View>
  );
};
