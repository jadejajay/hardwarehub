import * as React from 'react';

import { Text, View } from '@/ui';

type Props = {};
export const EditProduct = ({}: Props) => {
  return (
    <View className="flex-1">
      <Text className="text-base">EditProduct Component</Text>
    </View>
  );
};
