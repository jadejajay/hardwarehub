import * as React from 'react';

import { Text, View } from '@/ui';

type Props = {};
export const ProductFeed = ({}: Props) => {
  return (
    <View className="flex-1">
      <Text className="text-base">ProductFeed Component</Text>
    </View>
  );
};
