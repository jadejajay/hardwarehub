import * as React from 'react';

import { Text, View } from '@/ui';

type Props = {};
export const Activity = ({}: Props) => {
  return (
    <View className="flex-1">
      <Text className="text-base">Activity Component</Text>
    </View>
  );
};
