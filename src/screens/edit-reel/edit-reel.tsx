import * as React from 'react';

import { Text, View } from '@/ui';

type Props = {};
export const EditReel = ({}: Props) => {
  return (
    <View className="flex-1">
      <Text className="text-base">EditReel Component</Text>
    </View>
  );
};
