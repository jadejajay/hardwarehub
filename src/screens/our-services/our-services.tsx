import * as React from 'react';

import { Text, View } from '@/ui';

type Props = {};
export const OurServices = ({}: Props) => {
  return (
    <View className="items-center justify-center">
      <Text className="text-base">OurServices</Text>
    </View>
  );
};
