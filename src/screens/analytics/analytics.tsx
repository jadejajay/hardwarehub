import * as React from 'react';

import { Text, View } from '@/ui';

type Props = {};
export const Analytics = ({}: Props) => {
  return (
    <View className="flex-1">
      <Text className="text-base">Analytics Component</Text>
    </View>
  );
};
