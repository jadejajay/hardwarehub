import * as React from 'react';

import { Text, View } from '@/ui';

type Props = {};
export const Saved = ({}: Props) => {
  return (
    <View className="flex-1">
      <Text className="text-base">Saved Component</Text>
    </View>
  );
};
